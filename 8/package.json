{
  "name": "validator-system",
  "version": "1.0.0",
  "scripts": {
    "start": "npx ts-node validators/schema.ts",
    "dev": "npx ts-node --watch validators/schema.ts",
    "build": "npx tsc",
    "run": "node dist/schema.js",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "ts-node": "^10.9.0",
    "@types/node": "^20.0.0",
    "jest": "^29.0.0",
    "@types/jest": "^29.0.0",
    "ts-jest": "^29.0.0"
  }
} 